<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Profile - <%= username %></title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <header>
    <h1>Welcome, <%= username %>!</h1>
    <%- include('../partials/_navbar.ejs') %>
    <a href="/auth/sign-out">Logout</a>
    <a href="/spots">All Spots</a>
  </header>

  <section>
    <h2>Your Spots</h2>
    <% if (spots.length === 0) { %>
      <p>You have no spots yet.</p>
    <% } else { %>
      <ul>
        <% spots.forEach(spot => { %>
          <li>
            <a href="/spots/<%= spot._id %>"><%= spot.name %></a>
            <a href="/spots/<%= spot._id %>/edit">Edit</a>
            <form action="/spots/<%= spot._id %>?_method=DELETE" method="POST" style="display:inline;">
              <button type="submit">Delete</button>
            </form>
          </li>
        <% }) %>
      </ul>
    <% } %>
  </section>
</body>
</html>
